---
description: Scaffold new components, modules, or features following project conventions
allowed-tools: Read, Write, Bash(mkdir:*)
---

## Context

Existing project structure:
```
!`find . -type f \( -name "*.py" -o -name "*.js" -o -name "*.ts" \) | head -15`
```

## Your Task

Scaffold: $ARGUMENTS

Based on the existing project structure and conventions:

1. **Analyze existing patterns**
   - File naming conventions
   - Directory structure
   - Import patterns
   - Code style

2. **Create the scaffold**
   - Generate appropriate files
   - Include boilerplate code
   - Add necessary imports
   - Create test file stubs

3. **Follow conventions**
   - Match existing code style
   - Use consistent naming
   - Include proper documentation
   - Add type hints if applicable

4. **Provide guidance**
   - Explain what was created
   - Suggest next steps
   - Note any manual steps needed

Examples:
- `/scaffold component Button` - Creates a new UI component
- `/scaffold api users` - Creates API endpoint scaffolding
- `/scaffold service payment` - Creates a service module

